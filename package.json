{
  "name": "codeviz",
  "version": "0.1.0",
  "private": true,
  "description": "Dev tooling for the codeviz Claude Code plugin (tests only — the plugin itself is zero-build).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "elkjs": "^0.11.1"
  }
}
